name: CI

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]

permissions:
  contents: read

jobs:
  brew-build:
    name: Homebrew
    runs-on: macos-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v4
